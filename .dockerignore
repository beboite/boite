# Build artifacts and deps: rebuilt inside the image.
target
**/target
node_modules
**/node_modules
build
.svelte-kit

# VCS + local
.git
.github
.env
.env.*
*.log
boite-data
data
workspace

# Runtime mount dirs (created by docker-compose on the server, root-owned and
# unreadable to the build context). Never part of the image build.
claude
claude.json
codex
opencode
opencode-data
*.bak-*

# Desktop bundles, never needed by the server build.
src-tauri/gen
src-tauri/icons

# Android TWA wrapper: packaging only, irrelevant to the server image.
mobile
