{
  "name": "@boite/core",
  "version": "2.0.0-alpha.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/client.ts"
  },
  "scripts": {
    "start": "bun run bin/core.ts",
    "dev": "bun run --watch src/main.ts",
    "build": "bun build src/main.ts src/platform/jobs-worker.ts --target bun --outdir dist --splitting --sourcemap=none --entry-naming [name].[ext]",
    "build:exe": "bun run build && bun build --compile src/main.ts --outfile dist/boite-core.exe",
    "check": "tsc -p tsconfig.json",
    "test": "bun test"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.261",
    "@boite/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "typescript": "^5.9.0"
  }
}
