# Gradle / Android build output
.gradle/
build/
app/build/
local.properties

# Signing secrets — NEVER commit. Kept outside the repo.
*.keystore
*.jks
KEYSTORE_PASSWORD.txt

# Build artifacts (regenerated by `gradlew bundleRelease` / Bubblewrap)
*.aab
*.apk
*.apks
*.idsig
