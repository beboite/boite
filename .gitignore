.DS_Store
node_modules
/target
/build
/.svelte-kit
/package
/release-builds
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Built by scripts/build-sidecar.mjs before each bundle; carries the target
# triple in its name and is never a source artifact.
/src-tauri/binaries/

# Written by a locally started boite-server (smoke runs). Holds a database, transcripts
# and a VAPID keypair, none of which belong to anybody but the machine that ran it.
boite-data/

# Third-party plugins cloned or installed into the workspace. They belong to
# whoever wrote them and carry their own history; a copy checked in here goes
# stale the day upstream moves.
/plugins/
