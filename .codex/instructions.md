The guidelines live in [`.agents/instructions.md`](../.agents/instructions.md).

Kept as a pointer rather than a copy: two copies of the same rules drift, and
the stale one is the one somebody follows.
